// Função para a sequência (a) - Números ímpares
function nextA() {
    document.getElementById("seq-a").innerText = 9;
}

// Função para a sequência (b) - Progressão geométrica (x2)
function nextB() {
    document.getElementById("seq-b").innerText = 128;
}

// Função para a sequência (c) - Quadrados perfeitos
function nextC() {
    document.getElementById("seq-c").innerText = 49;
}

// Função para a sequência (d) - Quadrados de números pares
function nextD() {
    document.getElementById("seq-d").innerText = 100;
}

// Função para a sequência (e) - Sequência de Fibonacci
function nextE() {
    document.getElementById("seq-e").innerText = 13;
}

// Função para a sequência (f) - Números consecutivos a partir de 12
function nextF() {
    document.getElementById("seq-f").innerText = 20;
}
